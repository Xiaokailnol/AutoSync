name: openwrt_ipk

on:
  repository_dispatch:
  workflow_dispatch:

jobs:
  build:
    name: Build and Sync IPK
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target:
          - aarch64_generic
          - x86_64
        sdk:
          - 24.10.5

    steps:
    - name: Checkout (for safety)
      uses: actions/checkout@v3

    - name: Sync IPK
      run: |
        set -e
        latest_packages_release=$(curl -s "https://api.github.com/repos/Xiaokailnol/openwrt_packages/releases/latest" | grep '"tag_name":' | sed -E 's/.*"([^"]+)".*/\1/')
        echo "Latest release: $latest_packages_release"
        latest_helloworld_release=$(curl -s "https://api.github.com/repos/Xiaokailnol/openwrt_helloworld/releases/latest" | grep '"tag_name":' | sed -E 's/.*"([^"]+)".*/\1/')
        echo "Latest release: $latest_helloworld_release"
        curl -L -o "packages-${{ matrix.target }}-${{ matrix.sdk }}.zip" "https://github.com/Xiaokailnol/openwrt_packages/releases/download/$latest_packages_release/${{ matrix.target }}-${{ matrix.sdk }}.zip"
        curl -L -o "helloworld-${{ matrix.target }}-${{ matrix.sdk }}.zip" "https://github.com/Xiaokailnol/openwrt_helloworld/releases/download/$latest_helloworld_release/${{ matrix.target }}-${{ matrix.sdk }}.zip"
        mkdir -p sdk
        unzip -o "packages-${{ matrix.target }}-${{ matrix.sdk }}.zip" -d sdk
        unzip -o "helloworld-${{ matrix.target }}-${{ matrix.sdk }}.zip" -d sdk
        rm -f "packages-${{ matrix.target }}-${{ matrix.sdk }}.zip"
        rm -f "helloworld-${{ matrix.target }}-${{ matrix.sdk }}.zip"

    - name: Git push
      run: |
        set -e
        cd sdk

        # 初始化 git，如果已经是仓库就跳过
        if [ ! -d .git ]; then
          git init
        fi

        git config user.name "github-actions"
        git config user.email "actions@github.com"

        # 设置分支名为 matrix.target
        git branch -M ${{ matrix.target }} || true

        # 设置远程 origin
        git remote add origin https://github.com/Xiaokailnol/Extras-packages.git 2>/dev/null || true
        git remote set-url origin https://${{ github.actor }}:${{ secrets.ACCESS_TOKEN }}@github.com/Xiaokailnol/Extras-packages.git

        git add .

        Emoji=("🎉" "🤞" "✨" "🎁" "🎈" "🎄" "🎨" "💋" "🍓" "🍕" "🍉" "💐" "🌴" "🚀" "🛸" "🗽" "⛅" "🌈" "🔥" "⛄" "🐶" "🏅" "🦄" "🐤")

        # 只有有变更才 commit
        if ! git diff-index --quiet HEAD --; then
          git commit -m "${Emoji[$RANDOM % ${#Emoji[@]}]} Sync $(date '+%Y-%m-%d %H:%M:%S')"
          git push -f origin ${{ matrix.target }}
        else
          echo "No changes to commit."
        fi
